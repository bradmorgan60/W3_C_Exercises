#include <stdio.h>

int main() {
    int num = 1; 

    /* 
    The integer '1' was just converted into 32 bits
    32 bits for an integer is common practice

    32 bits = 4 bytes

    That means that one integer represents 4 bytes in memory

    So...does that mean a memory address consists of 4 bytes?

    */


    int nums[5], array_size;

    array_size = sizeof(nums) / sizeof(int);



    
    
    
    return 0;
}
